version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: raw_gz_sales
      - name: product
        identifier: raw_cc_product
      - name: ship
        identifier: raw_cc_ship